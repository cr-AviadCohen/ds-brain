[project]
name = "ds-brain-tools"
version = "0.1.0"
description = "Lint and ingest helpers for ds-brain"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "rich>=13.0",
    "python-docx>=1.1",
    "openpyxl>=3.1",
]

[tool.uv]
dev-dependencies = ["pytest>=8.0"]
