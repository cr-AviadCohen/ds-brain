[project]
name = "ds-brain-server"
version = "0.1.0"
description = "Scheduled automation runtime for ds-brain (auto-ingest, future source pullers)"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
