# LLM Experiment Results.xlsx

## Sheet3
| Task | Sub-Task | Executor | Dataset | Dataset Size | Dataset Name | Dataset Distribution | Model ID | Fine-Tuning | Training Conf | Comments | Code | Training Time | Training Loss | Eval Loss | AUC | Threshold | TPR | FPR | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PowerShell Classification | Classification | Aviad Cohen | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | meta-llama/Meta-Llama-3-8B-Instruct | Yes - using train & val data | import time\nimport torch\nimport os\nfrom pathlib import Path\nfrom transformers import (\n    AutoTokenizer,\n    AutoConfig,\n    AutoModelForCausalLM,\n    Trainer,\n    TrainingArguments,\n    DataCollatorForLanguageModeling, EarlyStoppingCallback,\n)\nfrom peft import LoraConfig, get\_peft\_model, prepare\_model\_for\_kbit\_training\nfrom transformers import BitsAndBytesConfig, TrainerCallback\nfrom sklearn.metrics import (\n    precision\_recall\_fscore\_support,\n    accuracy\_score,\n    roc\_auc\_score,\n    confusion\_matrix\n)\nimport numpy as np\nimport shutil\nfrom scipy.special import softmax\n\n# Add NumPy core.multiarray.\_reconstruct to safe globals for checkpoint loading\nfrom torch.serialization import add\_safe\_globals\nadd\_safe\_globals(['\_reconstruct'])\n\n# ========== PYTORCH CONF ========== #\nos.environ["PYTORCH\_CUDA\_ALLOC\_CONF"] = "expandable\_segments:True"\n\n# ========== VERIFY GPU ========== #\nassert torch.cuda.is\_available(), "CUDA GPU not available!"\ndevice = "cuda:0"\n\n# ========== CONFIG ========== #\nMODEL\_DIR = MODEL\nOUTPUT\_DIR = MODEL.replace('/', '\_\_')\nos.makedirs(OUTPUT\_DIR, exist\_ok=True)\n\n\n# ========== CALLBACKS ========== #\nclass CleanupCallback(TrainerCallback):\n    def on\_epoch\_end(self, args, state, control, \*\*kwargs):\n        torch.cuda.empty\_cache()\n        \n    def on\_step\_end(self, args, state, control, \*\*kwargs):\n        # if state.global\_step % 50 == 0: torch.cuda.empty\_cache()\n        ...\n\n\n# class LossTrackerCallback(TrainerCallback):\n#     def \_\_init\_\_(self):\n#         self.train\_losses = []\n#         self.eval\_losses = []\n#         self.steps = []\n#\n#     def on\_log(self, args, state, control, logs=None, \*\*kwargs):\n#         step = state.global\_step\n#         if logs is None:\n#             return\n#         if "loss" in logs:\n#             self.train\_losses.append(logs["loss"])\n#             self.steps.append(step)\n#         if "eval\_loss" in logs:\n#             self.eval\_losses.append(logs["eval\_loss"])\n# loss\_tracker = LossTrackerCallback()\n\n\n# ========== QUANTIZATION CONFIG ========== #\nbnb\_config = BitsAndBytesConfig(\n    load\_in\_4bit=True,\n    bnb\_4bit\_quant\_type="nf4",\n    bnb\_4bit\_use\_double\_quant=True,\n    bnb\_4bit\_compute\_dtype=torch.bfloat16,\n)\n\n# ========== LOAD MODEL (force GPU) ========== #\nconfig = AutoConfig.from\_pretrained(MODEL\_DIR, trust\_remote\_code=True)\nconfig.attn\_implementation = "eager"\n# config.attn\_implementation = "flash\_attention\_2"\nbase\_model = AutoModelForCausalLM.from\_pretrained(\n    MODEL\_DIR,\n    config=config,\n    trust\_remote\_code=True,\n    device\_map={"": device},\n    quantization\_config=bnb\_config\n)\nbase\_model = prepare\_model\_for\_kbit\_training(base\_model)\nbase\_model.config.use\_cache = False\n\n# ========== LORA CONFIG ========== #\nlora\_config = LoraConfig(\n    r=16,\n    lora\_alpha=32,\n    target\_modules=[\n        "q\_proj",\n        "k\_proj",\n        "v\_proj",\n        "o\_proj",\n        "gate\_proj",\n        "up\_proj",\n        "down\_proj"\n    ],\n    lora\_dropout=0.05,\n    bias="none",\n    task\_type="CAUSAL\_LM"\n)\nmodel = get\_peft\_model(base\_model, lora\_config)\nmodel.print\_trainable\_parameters()\n\n# ========== OPTIMIZED TRAINING ARGS ========== #\ntraining\_args = TrainingArguments(\n    output\_dir=OUTPUT\_DIR,  # Increased batch size from 1 to 2 or 4 depending on GPU memory\n    per\_device\_train\_batch\_size=4,  # Try 2 if OOM errors occur\n    per\_device\_eval\_batch\_size=4,  # Try 2 if OOM errors occur\n    gradient\_accumulation\_steps=4,\n    #\n    num\_train\_epochs=3,\n    learning\_rate=2e-5,\n    warmup\_ratio=0.05,\n    weight\_decay=0.01,\n    lr\_scheduler\_type="linear", # "cosine" # "linear" is more stable than cosine for many tasks\n    #\n    save\_total\_limit=1,  # Keep only 1 checkpoint to save disk space\n    #\n    bf16=True,\n    fp16=False,\n    # Optimization\n    dataloader\_num\_workers=2,\n    gradient\_checkpointing=True,\n    #\n    load\_best\_model\_at\_end=True,\n    metric\_for\_best\_model="eval\_loss",  # or "eval\_loss", "Accuracy", etc.\n    greater\_is\_better=False,  # depends on your selected metric\n    #\n    save\_strategy="steps",\n    eval\_strategy="steps",  # "epoch" = run eval at end of each epoch | "steps" = run eval every N steps\n    logging\_strategy="steps",\n    #\n    eval\_steps=10,  # ← if using steps, pick a value\n    logging\_steps=10, # ← if using steps, pick a value\n)\n\n\n# ========== METRICS ========== #\ndef Metrics(eval\_pred):\n    logits, labels = eval\_pred\n\n    # ↓ only take first label token\n    if labels.ndim > 1:\n        labels = labels[:, 0]  # take only first token label\n\n    # same for logits if needed\n    if logits.ndim > 2:\n        logits = logits[:, 0, :]  # take only first token's logits\n\n    # Ensure logits and labels are numpy arrays\n    logits = np.array(logits)\n    labels = np.array(labels)\n\n    # Convert logits to class probabilities\n    probs = softmax(logits, axis=1)\n    preds = np.argmax(probs, axis=1)\n\n    # Make sure labels are integers (not one-hot or float)\n    if labels.ndim > 1:\n        labels = np.argmax(labels, axis=1)\n\n    # Compute metrics\n    try:\n        AUC = roc\_auc\_score(labels, probs[:, 1])\n    except ValueError:\n        AUC = float("nan")  # fallback if only one class in eval\n\n    # Compute TPR (Recall), FPR, AUC\n    CM = confusion\_matrix(labels, preds)\n    TN, FP, FN, TP = CM.ravel()\n    Accuracy = (TP + TN) / (TP + TN + FP + FN + 1e-12)\n    TPR = TP / (TP + FN + 1e-12)\n    FPR = FP / (FP + TN + 1e-12)\n    TNR = TN / (TN + FP + 1e-12)\n    FNR = FN / (FN + TP + 1e-12)\n    Precision = TP / (TP + FP + 1e-12)\n    IDR = TPR \* TNR\n    F1 = 2 \* (Precision \* TPR) / (Precision + TPR + 1e-12)\n\n    return {\n        "AUC": AUC,\n        "Accuracy": Accuracy,\n        "TPR": TPR,\n        "FPR": FPR,\n        "TPR\*TNR": IDR,\n        # "TNR": TNR,\n        # "FNR": FNR,\n        # "Precision": Precision,\n        # "F1": F1\n    }\n\n# ========== TRAINER ========== #\ntrainer = Trainer(\n    model=model,\n    args=training\_args,\n    train\_dataset=tokenized\_dataset\_train,\n    eval\_dataset=tokenized\_dataset\_valid,  # Use validation dataset for evaluation during training\n    tokenizer=tokenizer,\n    \n    data\_collator=DataCollatorForLanguageModeling(tokenizer=tokenizer, mlm=False),\n    # compute\_metrics=Metrics,\n    callbacks=[\n        CleanupCallback(),\n        EarlyStoppingCallback(early\_stopping\_patience=2),  # or any value suitable for you\n        # loss\_tracker,\n    ]\n)\n\n# ========== CLEANUP ========== #\ndef cleanup\_checkpoints(output\_dir: str):\n    print("🧹 Cleaning up intermediate checkpoints...")\n    for item in os.listdir(output\_dir):\n        if item.startswith("checkpoint-"):\n            checkpoint\_path = os.path.join(output\_dir, item)\n            try:\n                shutil.rmtree(checkpoint\_path)\n                print(f"✅ Removed: {checkpoint\_path}")\n            except Exception as e:\n                print(f"⚠️ Failed to remove {checkpoint\_path}: {e}")\n\ndef cleanup\_huggingface\_cache():\n    cache\_dir = os.path.expanduser("~/.cache/huggingface/transformers")\n    if os.path.exists(cache\_dir):\n        print("🧹 Optionally cleaning Hugging Face transformers cache...")\n        try:\n            shutil.rmtree(cache\_dir)\n            print(f"✅ Removed: {cache\_dir}")\n        except Exception as e:\n            print(f"⚠️ Failed to remove {cache\_dir}: {e}")\n\n# ========== CHECKPOINTS - SAFE RESUME ========== #\nUSE\_CHECKPOINT = False\nlast\_checkpoint = None\noutput\_dir\_path = Path(OUTPUT\_DIR)\nif USE\_CHECKPOINT and output\_dir\_path.exists():\n    checkpoints = sorted(output\_dir\_path.glob("checkpoint-\*"), key=lambda x: int(x.name.split("-")[-1]))\n    if checkpoints:\n        last\_checkpoint = str(checkpoints[-1])\n        print(f"🔁 Found checkpoint at: {last\_checkpoint} — Resuming training...")\n\n# ========== TRAIN THE MODEL ==========\nprint("🚀 Starting/resuming optimized training on GPU...")\ntrain\_result = None\ntry:\n    torch.cuda.empty\_cache()\n    t0 = time.time()\n    train\_result = trainer.train(resume\_from\_checkpoint=last\_checkpoint)\n    training\_time\_minutes = int((time.time() - t0) / 60)\n    print("✅ Training completed successfully!")\n    print(f"⏱️ Training time: {training\_time\_minutes} minutes")\nexcept Exception as e:\n    print(f"⚠️ Error during initial training attempt: {e}")\n    # Example of retrying with a smaller batch size on OOM error, though this is a simplistic approach.\n    # A more robust solution would involve more specific error checking (e.g., for CUDA OOM).\n    if "out of memory" in str(e).lower():\n        print("Try with a smaller batch size:")\n        print(f"\t- trainer.args.per\_device\_train\_batch\_size = 1")\n        print(f"\t- trainer.args.per\_device\_eval\_batch\_size = 1")\n    else:\n        print(f"❌ Training failed with an unexpected error: {e}")\n\n# ========== SAVE ========== #\nif not train\_result:\n    print("❌ Training did not complete successfully. Skipping model saving and plotting.")\nelse:\n    print("💾 Saving final fine-tuned model...")\n    model.save\_pretrained(OUTPUT\_DIR)\n    tokenizer.save\_pretrained(OUTPUT\_DIR)\n    print("✅ Done!") | AutoModelForCasualLM | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/tree/research-notebooks/llm\_train/src/classification\_powershell/sft.ipynb | NaN | 0.7000 | 0.6 | 0.400000 | 0.914 | 0.5000 | 0.5800 | NaN |
| PowerShell Classification | Classification | Aviad Cohen | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | meta-llama/Llama-3.2-1B-Instruct | Yes - using train & val data | import time\nimport torch\nimport os\nfrom pathlib import Path\nfrom transformers import (\n    AutoConfig,\n    AutoModelForSequenceClassification,\n    Trainer,\n    TrainingArguments,\n    DataCollatorForLanguageModeling,\n    EarlyStoppingCallback,\n)\nfrom peft import LoraConfig, get\_peft\_model, prepare\_model\_for\_kbit\_training, TaskType\nfrom transformers import BitsAndBytesConfig, TrainerCallback\nfrom sklearn.metrics import (\n    precision\_recall\_fscore\_support,\n    accuracy\_score,\n    roc\_auc\_score,\n    confusion\_matrix\n)\nfrom transformers import DataCollatorWithPadding\nimport shutil\nfrom datetime import datetime\nimport numpy as np\nfrom scipy.special import softmax\nfrom torch.nn import CrossEntropyLoss\n\n# Add NumPy core.multiarray.\_reconstruct to safe globals for checkpoint loading\nfrom torch.serialization import add\_safe\_globals\n\nadd\_safe\_globals(['\_reconstruct'])\n\n# ========== PYTORCH CONF ========== #\nos.environ["PYTORCH\_CUDA\_ALLOC\_CONF"] = "expandable\_segments:True"\n\n# ========== VERIFY GPU ========== #\nassert torch.cuda.is\_available(), "CUDA GPU not available!"\ndevice = "cuda:0"\n\n# ========== CONFIG ========== #\nTIMESTAMP = datetime.now().strftime('%Y-%m-%d\_%H.%M.%S')\nOUTPUT\_DIR = f"finetuned\_model\_{MODEL.replace('/','\_')}\_{TIMESTAMP}"\nos.makedirs(OUTPUT\_DIR, exist\_ok=True)\nprint(f"Model: {MODEL}")\nprint(f"Output directory: {OUTPUT\_DIR}")\n\n# ========== QUANTIZATION CONFIG ========== #\nbnb\_config = BitsAndBytesConfig(\n    load\_in\_4bit=True,\n    bnb\_4bit\_quant\_type="nf4",\n    bnb\_4bit\_use\_double\_quant=True,\n    bnb\_4bit\_compute\_dtype=torch.bfloat16,\n)\n\n# ========== LOAD MODEL (force GPU) ========== #\nconfig = AutoConfig.from\_pretrained(\n    pretrained\_model\_name\_or\_path=MODEL,\n    trust\_remote\_code=True,\n    id2label={0: "ben", 1: "mal"},\n    label2id={"ben": 0, "mal": 1},\n    num\_labels=2,\n)\nconfig.attn\_implementation = "eager"\n# config.attn\_implementation = "flash\_attention\_2"\n\nbase\_model = AutoModelForSequenceClassification.from\_pretrained(\n    pretrained\_model\_name\_or\_path=MODEL,\n    quantization\_config=bnb\_config,\n    num\_labels=2,\n    id2label={0: "ben", 1: "mal"},\n    label2id={"ben": 0, "mal": 1},\n    trust\_remote\_code=True,\n    device\_map="auto",  # lets HF place layers on the A100\n    pad\_token\_id=tokenizer.pad\_token\_id,  # Important for per\_device\_train\_batch\_size/per\_device\_eval\_batch\_size > 1\n)\n\nprint("Preparing model for 4-bit training with gradient checkpointing...")\nbase\_model = prepare\_model\_for\_kbit\_training(\n    base\_model,\n    use\_gradient\_checkpointing=True,\n)\n\n# ========== LORA CONFIG ========== #\nlora\_config = LoraConfig(\n    task\_type=TaskType.SEQ\_CLS,\n    r=16,\n    lora\_alpha=32,\n    lora\_dropout=0.05,\n    bias="none",\n    target\_modules=[\n        "q\_proj",\n        "k\_proj",\n        "v\_proj",\n        "o\_proj",\n        "gate\_proj",\n        "up\_proj",\n        "down\_proj",\n    ],\n)\nmodel = get\_peft\_model(model=base\_model, peft\_config=lora\_config)\nmodel.print\_trainable\_parameters()\n\n# ========== OPTIMIZED TRAINING ARGS ========== #\ntraining\_args = TrainingArguments(\n    output\_dir=OUTPUT\_DIR,  # Increased batch size from 1 to 2 or 4 depending on GPU memory\n    per\_device\_train\_batch\_size=4,  # Try 2 if OOM errors occur\n    per\_device\_eval\_batch\_size=4,  # Try 2 if OOM errors occur\n    gradient\_accumulation\_steps=4,\n    #\n    num\_train\_epochs=5,\n    learning\_rate=5e-5,\n    warmup\_ratio=0.05,\n    weight\_decay=0.01,\n    lr\_scheduler\_type="linear",  # "cosine" # "linear" is more stable than cosine for many tasks\n    #\n    save\_total\_limit=1,  # Keep only 1 checkpoint to save disk space\n    #\n    bf16=True,\n    fp16=False,\n    # Optimization\n    dataloader\_num\_workers=2,\n    gradient\_checkpointing=True,\n    #\n    load\_best\_model\_at\_end=True,\n    metric\_for\_best\_model="eval\_loss",  # or "eval\_loss", "Accuracy", etc.\n    greater\_is\_better=False,  # depends on your selected metric\n    #\n    save\_strategy="steps",\n    eval\_strategy="steps",  # "epoch" = run eval at end of each epoch | "steps" = run eval every N steps\n    logging\_strategy="steps",\n    #\n    eval\_steps=10,  # ← if using steps, pick a value\n    logging\_steps=10,  # ← if using steps, pick a value\n    #\n    overwrite\_output\_dir=True,\n)\n\n\n# ========== METRICS ========== #\ndef Metrics(eval\_pred):\n    logits, labels = eval\_pred\n    probs = softmax(logits, axis=-1)\n    pos\_probs = probs[:, 1]  # probability of class “malicious”\n    preds = np.argmax(logits, axis=-1)\n\n    # Compute metrics\n    try:\n        AUC = roc\_auc\_score(y\_true=labels, y\_score=pos\_probs)\n    except ValueError:\n        AUC = float("nan")  # fallback if only one class in eval\n\n    # Compute TPR (Recall), FPR, AUC\n    CM = confusion\_matrix(y\_true=labels, y\_pred=preds)\n    TN, FP, FN, TP = CM.ravel()\n    Accuracy = (TP + TN) / (TP + TN + FP + FN + 1e-12)\n    TPR = TP / (TP + FN + 1e-12)\n    FPR = FP / (FP + TN + 1e-12)\n    TNR = TN / (TN + FP + 1e-12)\n    FNR = FN / (FN + TP + 1e-12)\n    Precision = TP / (TP + FP + 1e-12)\n    IDR = TPR \* TNR\n    F1 = 2 \* (Precision \* TPR) / (Precision + TPR + 1e-12)\n\n    return {\n        "AUC": AUC,\n        "Accuracy": Accuracy,\n        "TPR": TPR,\n        "FPR": FPR,\n        "TPR\*TNR": IDR,\n        # "TNR": TNR,\n        # "FNR": FNR,\n        # "Precision": Precision,\n        # "F1": F1\n    }\n\n\n# ========== CALLBACKS ========== #\nclass CleanupCallback(TrainerCallback):\n    def on\_epoch\_end(self, args, state, control, \*\*kwargs):\n        torch.cuda.empty\_cache()\n\n    def on\_step\_end(self, args, state, control, \*\*kwargs):\n        # if state.global\_step % 50 == 0: torch.cuda.empty\_cache()\n        ...\n\n\n# ========== TRAINER ========== #\ntrainer = Trainer(\n    model=model,\n    args=training\_args,\n    train\_dataset=tokenized\_dataset\_train,\n    eval\_dataset=tokenized\_dataset\_valid,  # Use validation dataset for evaluation during training\n    tokenizer=tokenizer,\n    data\_collator=DataCollatorWithPadding(tokenizer=tokenizer),\n    compute\_metrics=Metrics,\n    callbacks=[\n        CleanupCallback(),\n        EarlyStoppingCallback(early\_stopping\_patience=2),  # or any value suitable for you\n        # loss\_tracker,\n    ]\n)\n\n\ndef export\_trainer\_metadata(trainer, output\_dir: str, extra\_info: dict = None):\n    metadata = {\n        "timestamp": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n        "model\_class": trainer.model.\_\_class\_\_.\_\_name\_\_,\n        "model\_config": trainer.model.config.to\_dict(),\n        "tokenizer\_class": trainer.tokenizer.\_\_class\_\_.\_\_name\_\_ if trainer.tokenizer else None,\n        "training\_args": trainer.args.to\_dict(),\n        "train\_dataset\_size": len(trainer.train\_dataset) if trainer.train\_dataset else None,\n        "eval\_dataset\_size": len(trainer.eval\_dataset) if trainer.eval\_dataset else None,\n        "data\_collator": trainer.data\_collator.\_\_class\_\_.\_\_name\_\_ if trainer.data\_collator else None,\n        "callbacks": [cb.\_\_class\_\_.\_\_name\_\_ for cb in trainer.callback\_handler.callbacks],\n        "extra\_info": extra\_info or {}\n    }\n    output\_file\_path = os.path.join(output\_dir, "trainer\_metadata.json")\n    with open(output\_file\_path, 'w') as f:\n        json.dump(metadata, f, indent=3)\n        print(f"✅ Exported trainer metadata to: {output\_file\_path}")\n\n\n# ========== CLEANUP ========== #\ndef cleanup\_checkpoints(output\_dir: str):\n    print("🧹 Cleaning up intermediate checkpoints...")\n    for item in os.listdir(output\_dir):\n        if item.startswith("checkpoint-"):\n            checkpoint\_path = os.path.join(output\_dir, item)\n            try:\n                shutil.rmtree(checkpoint\_path)\n                print(f"✅ Removed: {checkpoint\_path}")\n            except Exception as e:\n                print(f"⚠️ Failed to remove {checkpoint\_path}: {e}")\n\n\ndef cleanup\_huggingface\_cache():\n    cache\_dir = os.path.expanduser("~/.cache/huggingface/transformers")\n    if os.path.exists(cache\_dir):\n        print("🧹 Optionally cleaning Hugging Face transformers cache...")\n        try:\n            shutil.rmtree(cache\_dir)\n            print(f"✅ Removed: {cache\_dir}")\n        except Exception as e:\n            print(f"⚠️ Failed to remove {cache\_dir}: {e}")\n\n\n# ========== CHECKPOINTS - SAFE RESUME ========== #\nUSE\_CHECKPOINT = False\nlast\_checkpoint = None\noutput\_dir\_path = Path(OUTPUT\_DIR)\nif USE\_CHECKPOINT and output\_dir\_path.exists():\n    checkpoints = sorted(output\_dir\_path.glob("checkpoint-\*"), key=lambda x: int(x.name.split("-")[-1]))\n    if checkpoints:\n        last\_checkpoint = str(checkpoints[-1])\n        print(f"🔁 Found checkpoint at: {last\_checkpoint} — Resuming training...")\n\n# ========== TRAIN THE MODEL ==========\nprint("🚀 Starting/resuming optimized training on GPU...")\ntrain\_result = None\ntry:\n    torch.cuda.empty\_cache()\n    t0 = time.time()\n    train\_result = trainer.train(resume\_from\_checkpoint=last\_checkpoint)\n    export\_trainer\_metadata(trainer=trainer, output\_dir=OUTPUT\_DIR)\n    training\_time\_minutes = int((time.time() - t0) / 60)\n    print("✅ Training completed successfully!")\n    print(f"⏱️ Training time: {training\_time\_minutes} minutes")\nexcept Exception as e:\n    print(f"⚠️ Error during initial training attempt: {e}")\n    # Example of retrying with a smaller batch size on OOM error, though this is a simplistic approach.\n    # A more robust solution would involve more specific error checking (e.g., for CUDA OOM).\n    if "out of memory" in str(e).lower():\n        print("Try with a smaller batch size:")\n        print(f"\t- trainer.args.per\_device\_train\_batch\_size = 1")\n        print(f"\t- trainer.args.per\_device\_eval\_batch\_size = 1")\n    else:\n        print(f"❌ Training failed with an unexpected error: {e}")\n\n# ========== SAVE ========== #\nif not train\_result:\n    print("❌ Training did not complete successfully. Skipping model saving and plotting.")\nelse:\n    print("💾 Saving final fine-tuned model to: '{OUTPUT\_DIR}'")\n    model.save\_pretrained(OUTPUT\_DIR)\n    tokenizer.save\_pretrained(OUTPUT\_DIR)\n    print("✅ Done!") | AutoModelForSequenceClassification | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/workspaces/auto-X/tree/research-notebooks/llm\_train/src/classification\_powershell/sft\_classification.ipynb | 29 minutes (early stopping) | 0.0700 | 0.16 | 0.983142 | 0.019 | 0.9660 | 0.0330 | NaN |
| PowerShell Classification | Classification | Aviad Cohen | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | Qwen/Qwen3-0.6B | Yes - using train & val data | import time\nimport torch\nimport os\nfrom pathlib import Path\nfrom transformers import (\n    AutoConfig,\n    AutoModelForSequenceClassification,\n    Trainer,\n    TrainingArguments,\n    DataCollatorForLanguageModeling,\n    EarlyStoppingCallback,\n)\nfrom peft import LoraConfig, get\_peft\_model, prepare\_model\_for\_kbit\_training, TaskType\nfrom transformers import BitsAndBytesConfig, TrainerCallback\nfrom sklearn.metrics import (\n    precision\_recall\_fscore\_support,\n    accuracy\_score,\n    roc\_auc\_score,\n    confusion\_matrix\n)\nfrom transformers import DataCollatorWithPadding\nimport shutil\nfrom datetime import datetime\nimport numpy as np\nfrom scipy.special import softmax\nfrom torch.nn import CrossEntropyLoss\n\n# Add NumPy core.multiarray.\_reconstruct to safe globals for checkpoint loading\nfrom torch.serialization import add\_safe\_globals\n\nadd\_safe\_globals(['\_reconstruct'])\n\n# ========== PYTORCH CONF ========== #\nos.environ["PYTORCH\_CUDA\_ALLOC\_CONF"] = "expandable\_segments:True"\n\n# ========== VERIFY GPU ========== #\nassert torch.cuda.is\_available(), "CUDA GPU not available!"\ndevice = "cuda:0"\n\n# ========== CONFIG ========== #\nTIMESTAMP = datetime.now().strftime('%Y-%m-%d\_%H.%M.%S')\nOUTPUT\_DIR = f"finetuned\_model\_{MODEL.replace('/','\_')}\_{TIMESTAMP}"\nos.makedirs(OUTPUT\_DIR, exist\_ok=True)\nprint(f"Model: {MODEL}")\nprint(f"Output directory: {OUTPUT\_DIR}")\n\n# ========== QUANTIZATION CONFIG ========== #\nbnb\_config = BitsAndBytesConfig(\n    load\_in\_4bit=True,\n    bnb\_4bit\_quant\_type="nf4",\n    bnb\_4bit\_use\_double\_quant=True,\n    bnb\_4bit\_compute\_dtype=torch.bfloat16,\n)\n\n# ========== LOAD MODEL (force GPU) ========== #\nconfig = AutoConfig.from\_pretrained(\n    pretrained\_model\_name\_or\_path=MODEL,\n    trust\_remote\_code=True,\n    id2label={0: "ben", 1: "mal"},\n    label2id={"ben": 0, "mal": 1},\n    num\_labels=2,\n)\nconfig.attn\_implementation = "eager"\n# config.attn\_implementation = "flash\_attention\_2"\n\nbase\_model = AutoModelForSequenceClassification.from\_pretrained(\n    pretrained\_model\_name\_or\_path=MODEL,\n    quantization\_config=bnb\_config,\n    num\_labels=2,\n    id2label={0: "ben", 1: "mal"},\n    label2id={"ben": 0, "mal": 1},\n    trust\_remote\_code=True,\n    device\_map="auto",  # lets HF place layers on the A100\n    pad\_token\_id=tokenizer.pad\_token\_id,  # Important for per\_device\_train\_batch\_size/per\_device\_eval\_batch\_size > 1\n)\n\nprint("Preparing model for 4-bit training with gradient checkpointing...")\nbase\_model = prepare\_model\_for\_kbit\_training(\n    base\_model,\n    use\_gradient\_checkpointing=True,\n)\n\n# ========== LORA CONFIG ========== #\nlora\_config = LoraConfig(\n    task\_type=TaskType.SEQ\_CLS,\n    r=16,\n    lora\_alpha=32,\n    lora\_dropout=0.05,\n    bias="none",\n    target\_modules=[\n        "q\_proj",\n        "k\_proj",\n        "v\_proj",\n        "o\_proj",\n        "gate\_proj",\n        "up\_proj",\n        "down\_proj",\n    ],\n)\nmodel = get\_peft\_model(model=base\_model, peft\_config=lora\_config)\nmodel.print\_trainable\_parameters()\n\n# ========== OPTIMIZED TRAINING ARGS ========== #\ntraining\_args = TrainingArguments(\n    output\_dir=OUTPUT\_DIR,  # Increased batch size from 1 to 2 or 4 depending on GPU memory\n    per\_device\_train\_batch\_size=4,  # Try 2 if OOM errors occur\n    per\_device\_eval\_batch\_size=4,  # Try 2 if OOM errors occur\n    gradient\_accumulation\_steps=4,\n    #\n    num\_train\_epochs=5,\n    learning\_rate=5e-5,\n    warmup\_ratio=0.05,\n    weight\_decay=0.01,\n    lr\_scheduler\_type="linear",  # "cosine" # "linear" is more stable than cosine for many tasks\n    #\n    save\_total\_limit=1,  # Keep only 1 checkpoint to save disk space\n    #\n    bf16=True,\n    fp16=False,\n    # Optimization\n    dataloader\_num\_workers=2,\n    gradient\_checkpointing=True,\n    #\n    load\_best\_model\_at\_end=True,\n    metric\_for\_best\_model="eval\_loss",  # or "eval\_loss", "Accuracy", etc.\n    greater\_is\_better=False,  # depends on your selected metric\n    #\n    save\_strategy="steps",\n    eval\_strategy="steps",  # "epoch" = run eval at end of each epoch | "steps" = run eval every N steps\n    logging\_strategy="steps",\n    #\n    eval\_steps=10,  # ← if using steps, pick a value\n    logging\_steps=10,  # ← if using steps, pick a value\n    #\n    overwrite\_output\_dir=True,\n)\n\n\n# ========== METRICS ========== #\ndef Metrics(eval\_pred):\n    logits, labels = eval\_pred\n    probs = softmax(logits, axis=-1)\n    pos\_probs = probs[:, 1]  # probability of class “malicious”\n    preds = np.argmax(logits, axis=-1)\n\n    # Compute metrics\n    try:\n        AUC = roc\_auc\_score(y\_true=labels, y\_score=pos\_probs)\n    except ValueError:\n        AUC = float("nan")  # fallback if only one class in eval\n\n    # Compute TPR (Recall), FPR, AUC\n    CM = confusion\_matrix(y\_true=labels, y\_pred=preds)\n    TN, FP, FN, TP = CM.ravel()\n    Accuracy = (TP + TN) / (TP + TN + FP + FN + 1e-12)\n    TPR = TP / (TP + FN + 1e-12)\n    FPR = FP / (FP + TN + 1e-12)\n    TNR = TN / (TN + FP + 1e-12)\n    FNR = FN / (FN + TP + 1e-12)\n    Precision = TP / (TP + FP + 1e-12)\n    IDR = TPR \* TNR\n    F1 = 2 \* (Precision \* TPR) / (Precision + TPR + 1e-12)\n\n    return {\n        "AUC": AUC,\n        "Accuracy": Accuracy,\n        "TPR": TPR,\n        "FPR": FPR,\n        "TPR\*TNR": IDR,\n        # "TNR": TNR,\n        # "FNR": FNR,\n        # "Precision": Precision,\n        # "F1": F1\n    }\n\n\n# ========== CALLBACKS ========== #\nclass CleanupCallback(TrainerCallback):\n    def on\_epoch\_end(self, args, state, control, \*\*kwargs):\n        torch.cuda.empty\_cache()\n\n    def on\_step\_end(self, args, state, control, \*\*kwargs):\n        # if state.global\_step % 50 == 0: torch.cuda.empty\_cache()\n        ...\n\n\n# ========== TRAINER ========== #\ntrainer = Trainer(\n    model=model,\n    args=training\_args,\n    train\_dataset=tokenized\_dataset\_train,\n    eval\_dataset=tokenized\_dataset\_valid,  # Use validation dataset for evaluation during training\n    tokenizer=tokenizer,\n    data\_collator=DataCollatorWithPadding(tokenizer=tokenizer),\n    compute\_metrics=Metrics,\n    callbacks=[\n        CleanupCallback(),\n        EarlyStoppingCallback(early\_stopping\_patience=2),  # or any value suitable for you\n        # loss\_tracker,\n    ]\n)\n\n\ndef export\_trainer\_metadata(trainer, output\_dir: str, extra\_info: dict = None):\n    metadata = {\n        "timestamp": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),\n        "model\_class": trainer.model.\_\_class\_\_.\_\_name\_\_,\n        "model\_config": trainer.model.config.to\_dict(),\n        "tokenizer\_class": trainer.tokenizer.\_\_class\_\_.\_\_name\_\_ if trainer.tokenizer else None,\n        "training\_args": trainer.args.to\_dict(),\n        "train\_dataset\_size": len(trainer.train\_dataset) if trainer.train\_dataset else None,\n        "eval\_dataset\_size": len(trainer.eval\_dataset) if trainer.eval\_dataset else None,\n        "data\_collator": trainer.data\_collator.\_\_class\_\_.\_\_name\_\_ if trainer.data\_collator else None,\n        "callbacks": [cb.\_\_class\_\_.\_\_name\_\_ for cb in trainer.callback\_handler.callbacks],\n        "extra\_info": extra\_info or {}\n    }\n    output\_file\_path = os.path.join(output\_dir, "trainer\_metadata.json")\n    with open(output\_file\_path, 'w') as f:\n        json.dump(metadata, f, indent=3)\n        print(f"✅ Exported trainer metadata to: {output\_file\_path}")\n\n\n# ========== CLEANUP ========== #\ndef cleanup\_checkpoints(output\_dir: str):\n    print("🧹 Cleaning up intermediate checkpoints...")\n    for item in os.listdir(output\_dir):\n        if item.startswith("checkpoint-"):\n            checkpoint\_path = os.path.join(output\_dir, item)\n            try:\n                shutil.rmtree(checkpoint\_path)\n                print(f"✅ Removed: {checkpoint\_path}")\n            except Exception as e:\n                print(f"⚠️ Failed to remove {checkpoint\_path}: {e}")\n\n\ndef cleanup\_huggingface\_cache():\n    cache\_dir = os.path.expanduser("~/.cache/huggingface/transformers")\n    if os.path.exists(cache\_dir):\n        print("🧹 Optionally cleaning Hugging Face transformers cache...")\n        try:\n            shutil.rmtree(cache\_dir)\n            print(f"✅ Removed: {cache\_dir}")\n        except Exception as e:\n            print(f"⚠️ Failed to remove {cache\_dir}: {e}")\n\n\n# ========== CHECKPOINTS - SAFE RESUME ========== #\nUSE\_CHECKPOINT = False\nlast\_checkpoint = None\noutput\_dir\_path = Path(OUTPUT\_DIR)\nif USE\_CHECKPOINT and output\_dir\_path.exists():\n    checkpoints = sorted(output\_dir\_path.glob("checkpoint-\*"), key=lambda x: int(x.name.split("-")[-1]))\n    if checkpoints:\n        last\_checkpoint = str(checkpoints[-1])\n        print(f"🔁 Found checkpoint at: {last\_checkpoint} — Resuming training...")\n\n# ========== TRAIN THE MODEL ==========\nprint("🚀 Starting/resuming optimized training on GPU...")\ntrain\_result = None\ntry:\n    torch.cuda.empty\_cache()\n    t0 = time.time()\n    train\_result = trainer.train(resume\_from\_checkpoint=last\_checkpoint)\n    export\_trainer\_metadata(trainer=trainer, output\_dir=OUTPUT\_DIR)\n    training\_time\_minutes = int((time.time() - t0) / 60)\n    print("✅ Training completed successfully!")\n    print(f"⏱️ Training time: {training\_time\_minutes} minutes")\nexcept Exception as e:\n    print(f"⚠️ Error during initial training attempt: {e}")\n    # Example of retrying with a smaller batch size on OOM error, though this is a simplistic approach.\n    # A more robust solution would involve more specific error checking (e.g., for CUDA OOM).\n    if "out of memory" in str(e).lower():\n        print("Try with a smaller batch size:")\n        print(f"\t- trainer.args.per\_device\_train\_batch\_size = 1")\n        print(f"\t- trainer.args.per\_device\_eval\_batch\_size = 1")\n    else:\n        print(f"❌ Training failed with an unexpected error: {e}")\n\n# ========== SAVE ========== #\nif not train\_result:\n    print("❌ Training did not complete successfully. Skipping model saving and plotting.")\nelse:\n    print("💾 Saving final fine-tuned model to: '{OUTPUT\_DIR}'")\n    model.save\_pretrained(OUTPUT\_DIR)\n    tokenizer.save\_pretrained(OUTPUT\_DIR)\n    print("✅ Done!") | AutoModelForSequenceClassification | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/workspaces/auto-X/tree/research-notebooks/llm\_train/src/classification\_powershell/sft\_classification.ipynb | 51 minutes (early stopping) | 0.1476 | 0.128373 | 0.980000 | 0.800 | 0.9780 | 0.0104 | NaN |
| PowerShell Classification | Classification | Itamar Hershko | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | codellama/CodeLlama-7b-Instruct-hf | Yes - using train & val data | NaN | Train on actual scripts | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/tree/Itamar/Main\_Yara\_Automation.ipynb | NaN | NaN | NaN | NaN | NaN | NaN | NaN | NaN |
| PowerShell Classification | Classification | Itamar Hershko | mpsd | 8000 | Benign Powershell/Malicious Powershell | Train: 3313 | Val: 414 | Test: 414 | codellama/CodeLlama-7b-Instruct-hf | Yes - using train & val data | NaN | Train on scripts extracted from malicious/benign ps1 files | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/tree/Itamar/LlamaCode\_7B\_Python.ipynb | ~3:00h | 0.0394 | - | 0.861800 | 0.660 | 0.9642 | 0.4382 | New test on a different data set (6k malicious 6k benign) |
| PowerShell Classification | Classification | Guy Michaela Kassorla | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | meta-llama/Llama-3.2-1B-Instruct | No | Pre-trained | NaN | NaN | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/workspaces/auto-g/tree/research-notebooks/llm\_train/src/classification\_powershell | NaN | NaN | NaN | NaN | NaN | 0.4820 | 0.6890 | NaN |
| PowerShell Classification | Classification | Guy Michaela Kassorla | mpsd | 8000 | mpsd\_\_\_LABELED\_\_(7990,8)\_\_2025-05-06\_15.07.09\_\_2025-05-13\_10.22.17\_\_prompt\_completion\_classification.jsonl | Train: 3313 | Val: 414 | Test: 414 | teknium/OpenHermes-2.5-Mistral-7B | No | Pre-trained | NaN | NaN | https://7c5f06735f1cd82-dot-us-central1.notebooks.googleusercontent.com/lab/workspaces/auto-g/tree/research-notebooks/llm\_train/src/classification\_powershell | NaN | NaN | NaN | NaN | NaN | 0.4270 | 0.3380 | NaN |
