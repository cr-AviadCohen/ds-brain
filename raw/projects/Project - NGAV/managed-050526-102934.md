# ~~-~~ PET automated model generator . net logic, distinction by

This pipeline job : https://irelease ~~.~~ eng ~~.~~ cybereason ~~.~~ [net:8443/job/ngav](https://irelease.eng.cybereason.net:8443/job/ngav-model-generator/) ~~-~~ model ~~-~~ generator/ is the


new automated way to train an NGAV model ~~.~~


The models will be stored at the end of the job in this artifactory designated directory:



gcp ~~.~~ eng ~~.~~ cybereason ~~.~~ [net/artifactory/webapp/#/artifacts/browse/tree/General/ngav](https://artifactory-gcp.eng.cybereason.net/artifactory/webapp/#/artifacts/browse/tree/General/ngav-pipeline) ~~-~~ pipeline ~~.~~


The pipeline is built in a way that every run is a whole training process,


collecting the data, generating features,


each run can be either managed / unmanaged petype ~~.~~

# pipeline structure:


All of the stages are running on a linux slave, except for one stage: generate features ~~.~~


this specific stage will be triggering another job, that will be running on a windows slave ~~.~~


This branching out is a ‘stool’ (the tool generating features behind the scenes) requirement ~~.~~


there are features that are depending on a windows OS environment ~~.~~


# pipeline stages:

An overview on each stage, it’s main goals, it’s inputs and outputs ~~.~~


in case the user selected a certain stage to start at (future feature), this stage will be idling ~~.~~


otherwise, this stage in clearing the workspace for this new run ~~.~~


fetching code from remote repository ~~.~~


notice, the pipeline’s code is running using the code in the ‘research’ repository, on a feature


there is also a feature branch called “pet ~~-~~ model ~~-~~ generator ~~-~~ dev”, where changes should be


implemented, tested and only then merged into the production branch ~~.~~


fetching the code from this particular branch, gains us both code stability and comfortability on


version management ~~.~~


https://github ~~.~~ com/cybereason ~~-~~ [labs/research/tree/pet](https://github.com/cybereason-labs/research/tree/pet-model-generator-prod) ~~-~~ model ~~-~~ generator ~~-~~ prod


https://github ~~.~~ com/cybereason ~~-~~ [labs/research/tree/pet](https://github.com/cybereason-labs/research/tree/pet-model-generator-dev) ~~-~~ model ~~-~~ generator ~~-~~ dev


<petype>_<major ~~.~~ minor>_<date>,


i ~~.~~ e ~~.~~ : unmanaged_3 ~~.~~ 2_20 10 2021 ~~.~~


major stands for code changes, minor for trained model version ~~.~~


this ID will be (a part of) the names of all artifacts that are generated in each stage ~~.~~


At this stage, exe and dll datasets are being created, using build_dataset script ~~.~~


these datasets will get divided later on, at the stage ‘data_preparation’, to the following datasets:


training set (a combination of both exe and dll),


exe, dll test sets,


exe, dll validation sets ~~.~~


exe samples are post bd, meaning that the samples we’re working with are samples that


BitDefender did NOT convict ~~.~~


dll samples are not post bd, meaning that we’re using all kinds of samples, without


differentiating between post ~~-~~ bd or not post ~~-~~ bd files ~~.~~


this is implemented by ignoring the “post bd” flag if used, when sampling for dll datasets ~~.~~


In addition, an allowlist potential dataset is generated using GlobalAllowlistGeneration


(ngav ~~.~~ model_optimization ~~.~~ allowlist ~~.~~ allowlisting) ~~.~~


All datasets are queried from this big query table:


~~-~~ ~~-~~ ~~-~~
vt feed pipeline acfe9f.vt_file_report_feed.vt_file_report_feed_1,


which gets updated daily with new scans from ‘virustotal’ ~~.~~


each row in this table represents a Virus Total scan, and has it’s unique scan_id, meaning one


file’s sha1 can show up more then one time, once for each VT scan ~~.~~


in the query, we are filtering our datasets so that each sampled file will show up only once, using


the latest appearance in the date frame we are sampling from ~~.~~


this stage is running some data quality checks and modifications, for both exe and dll datasets ~~.~~


among those actions:


making sure there are no nans in the features the model sees,


removing duplicated by feature vector samples,


removing duplicated by index samples,


making sure the labels and the features are aligned perfectly,


forcing 50% 50% distribution (malicious / benign wise),


making sure that the amount of files we are losing in these actions is acceptable,


splitting the exe, dll datasets both, to: training samples, validation samples, test samples ~~.~~


the validation and test dataset sizes are configured at the configuration file, and the priority in


splitting is for these datasets ~~.~~


the way in which we split the data to the three datasets, is as follows: (explaining over exe,


but same for dll)


with the amount of samples wanted for exe validation dataset size as n_validation, and the


amount of samples wanted for exe test dataset size as n_test


dividing the big exe dataset to malicious_samples, and benign_samples, and sorting it by


the scan date ~~.~~


building exe test set using:


the n_test/2 latest samples from malicious_samples,


and the n_test/2 latest samples from benign_samples ~~.~~


building exe validation set using:


the next n_validation/2 latest samples from malicious_samples,


and the next n_validation/2 latest samples frm benign_samples ~~.~~


the remaining samples (that are also distributed by label) goes to the training set, that is a


mix of the two ~~.~~ the model is training using both exe and dll files, and then specifies in each


of those when defining thresholds (and calculating KPIs on the test sets) ~~.~~


calculating weights for the training set, to be used in the model training stage ~~.~~





this stage is running another job:



https://irelease ~~.~~ eng ~~.~~ [cybereason](https://irelease.eng.cybereason.net:8443/job/ngav-generate-features-windows/28/) ~~.~~ net:8443/job/ngav ~~-~~ generate ~~-~~



(35 ~~.~~ 231 ~~.1~~ 14 ~~.1~~ 85) ~~.~~



that is using featuregen (‘stool’ python wrapper) on a windows slave server



featuregen will generate for each (dataset X ~~.~~ index ~~.~~ pkl),(petype un/managed) three files:


<petype>_X ~~.~~ index ~~.~~ pkl ~~.~~ pklbz2 ~~-~~ the main file of features,


<petype>_X ~~.~~ index ~~.~~ pkl_extracols ~~.~~ pklbz2 ~~-~~ additional features columns,


<petype>_X ~~.~~ index ~~.~~ pkl_rest ~~.~~ pklbz2 ~~-~~ this file contains all the samples that got an error message


in the process of generating it’s features ~~.~~


This stage must run on a windows OS, because some of the features are involving the OS in it’s


calculations, and depends on the fact that the OS is windows ~~.~~


Therefore, this stage will run on a windows slave ~~.~~


this transition from linux to windows and vice versa must be secured, as the windows machine


which is generating the features is at risk, due to the fact that it downloads files that may be


malicious ~~.~~ for that reason, we created an mediator ~~.~~ the inputs and output of ‘generate features’


stage, will be copied to and from this GCS bucket:



https://console ~~.~~ cloud ~~.~~ google ~~.~~ [com/storage/browser/lin](https://console.cloud.google.com/storage/browser/lin-win-ngav;tab=objects?authuser=0&project=cr-core-dev-host-project-fe70&prefix=&forceOnObjectsSortingFiltering=false) ~~-~~ win ~~-~~ ngav



~~.~~



This stage is generating features for both datasets created in the last step, using


generate_features ~~.~~ py that runs stool (our feature generator) ~~.~~


Once all the files are in the input folder, featuregen is running on each one of them, one at a


This is generating a x ~~.~~ index ~~.~~ pkl ~~.~~ pklbz2, x ~~.~~ index ~~.~~ pkl_rest ~~.~~ pklbz2 ~~.~~ for each x file in the input folder ~~.~~


for each dataset input, stool will split it’s results to managed and unmanaged outputs ~~.~~ for eac


run type (un/managed), we will take only the relevant output files ~~.~~


these relevant outputs for each dataset (ID_dataset_x ~~.~~ index ~~.~~ pkl, whitelist_potential_<filestype>)


are being copied into the workspace/input directory ~~.~~ (“featuregen” requirment) ~~.~~


The generated files are located in the workspace/output folder, and being transferred via GCP


bucket to the pipeline’s linux slave ‘workspace’ directory ~~.~~





Training stage, using the crossguard ~~.~~ train module ~~.~~


this stage is building a sklearn Pipeline, containing all transformers and the estimator


the logic:


Using the trained model (classifier object), validation features set, validation vtindex set, this


stage’s purposes are to define three thresholds: aggressive, moderate, cautious and create


evaluation plots of the trained model, for both exe and dll, using the respective validation set


creates earlier ~~.~~


(such as: metrics vs threshold, FPR, TP rate, FP rate, pos rate, engines rates, warnings in case of


threshold crossing it’s defined limit, thresholds dataframe)


the thresholds are being selected with respect to the requirements defined (in


model_optimization ~~.~~ threshold_optimization module) for each of the file types ~~.~~




now, that we have thresholds in hand,


this stage puts these thresholds to the test, by calculating metrics on the test set (samples the


model didn’t see yet), for the purpose of later comparing it to the validation set’s metrics that


was used to determine these thresholds, to check how the model is performing on unseen data ~~.~~


The comparison is made by allowing a certain gap from the validation set metrics ~~.~~


the standard for valid test metrics is:


test_recall >= validation_recall - 0 ~~.~~ 02


test_FPR <= validation_FPR - 0 ~~.1~~


In addition, this stage is adding the metrics information to the pdf plots created in the ‘threshold




Using the GlobalAllowlistGeneration class ( ~~.~~ create_allowlist()), the aggressive threshold, and the


‘allowlist_potential_<files_type>’ artifact from ‘create datasets’ stage, this stage is generating a


final allowlist ~~.~~


We want to generate an allowlist for each one of the types (un/managed) and then combine


them together to one all inclusive allowlist ~~.~~


for every petype (unmanaged / managed, exe / dll) there are different thresholds ~~.~~


The flow is as follows, assuming this run is unmanaged:


1. allowlist potential (with no distinction between un/managed, exe/dll) is being generated


(create datasets stage)


2. features are being generated for allowlist potential (generate features stage)


3. the relevant pe type (unmanaged) only is being taken from the featuregen output (this output


is holding both exe and dll samples) ~~.~~


4. two GlobalAllowlistGeneration instances are generated, one for exe, and one for dll ~~.~~ each one


gets the proper aggressive threshold ~~.~~


5. two separate allowlists are being generated, both from the same petype (unmanaged) ~~.~~ The


combination of the two is what we’ll save on disc and call “unmanaged_allowlist ~~.~~ csv “(that’s


for the next run of managed, that will be using this unmanaged allowlist to combine it with the


managed allowlist that will be generated in that run ~~.~~ )


6. now all we need is to attach the unmanaged_allowlist ~~.~~ csv to the very same one that was


generated the last time that the other petype (managed) pipeline ran ~~.~~ i ~~.~~ e ~~.~~ taking the latest


“managed_allowlist ~~.~~ csv” from the artifactory, and combine it with the unmanaged_allowlist


(generate allowlist lines 83 97)


Notice, this solution is depending on the fact that each of these pipeline runs will generate the


relevant petype allowlist ~~.~~




whitelist_final_{files_type} ~~.~~ pkl


At this stage, the AUC score of both the previous published model and the current model is


calculated, logged and compared ~~.~~


In case the (exe) current model’s AUC is lower than the (exe) production model, the pipeline will


be terminated here, and as a result, this model will not be uploaded to the artifactory ~~.~~ (unless the


‘upload ~~-~~ lower ~~-~~ auc ~~-~~ to ~~-~~ artifacroty’ flag is set ~~.~~ in that case, this pipeline will not be terminated and


the model will be uploaded regardless to it’s AUC score) ~~.~~


Extraction of some elements from the classifier (using protect ~~.~~ exportmodel module), in order to


allow it to be imported as a model ~~.~~





This stage stays in the groovy code level ~~.~~


‘upload_to_artifactory’ function loops over all of the files in the working directory


(/opt/ ~~...~~ /{model_ID}) and upload them to a <unmanaged / managed> folder in the artifactory



named as the model’s ID, under:





gcp ~~.~~ eng ~~.~~ cybereason ~~.~~ [net/artifactory/webapp/#/artifacts/browse/tree/General/ngav](https://artifactory-gcp.eng.cybereason.net/artifactory/webapp/#/artifacts/browse/tree/General/ngav-pipeline) ~~-~~ pipeline ~~.~~


# pipeline arguments:

An overview on the main arguments the pipeline is based on ~~.~~


~~-~~

ID type_major ~~.~~ minor_date,


for example: unmanaged_3 ~~.~~ 2_20 10 2021 ~~.~~


major stands for code changes, minor for model version with <major> code version ~~.~~


ID is a unique identifier for the current training model ~~.~~ that ID is being generated at the


beginning of each pipeline run and will be of use on every step of the way, and will construct the


names of all artifacts generated by the pipeline in addition to the appropriate extensions ~~.~~


~~-~~ ~~-~~

base_dir the working directory of the current pipeline run, i ~~.~~ e ~~.~~ ‘/opt/jenkins/workspace/ngav


~~-~~

log_file_path the log file path to which all stages will log ~~.~~ (<base_dir>/logger ~~.~~ txt)


this job is getting a few key parameters (with default values) from the user :


files_type ~~-~~ unmanaged / managed ~~.~~


exe_datasets_dates_range ~~-~~ exe training+validation+test datasets date range ~~.~~ _ _ ~~.~~


_ _ means that the dates were not entered by the user ~~.~~


in case even one of the dates are blank, the pipeline is generating the dates automatically ~~.~~


the dates picking automation process is determined in the configuration file, and is different for


each one of the types, according to our experience with sampling rates for each type ~~.~~


if user wishes to manually define the dates, he can do so ~~.~~


for example: 20210101 20210430 means that the starting date is January 1st, 2021, and the end


date is April 30, 2021 ~~.~~


dll_datasets_dates_range ~~-~~ dll training+validation+test datasets date range ~~.~~ _ _ ~~.~~


training_samples_amount ~~-~~ wanted training dataset size ~~.~~ (exe_dll samples) 1000000 ~~.~~


conditions ~~-~~ zero or more flags that allows the user to control and manipulate the way datasets


are sampled, options are:


cond post bd ; only select files not convicted by BitDefender,


cond replace bd ; only select files convicted by BitDefender (or indifferent),


cond stark labels ; only take stark labels (ignore "unwanted" and other deprecated junk


cond ransomware ; only keep samples labeled as "ransomware" or "indifferent",


cond dotnet ; only select files with "assembly" tag,


~~--~~ cond ~~-~~ for ~~-~~ cert ~~-~~ test ; drop samples of which their label is of low consensus on vt ~~.~~ good for


training a model for certification tests ~~.~~


by default, the pipeline is calculating the auc score of the current model, and the auc score of


the last published model and compares the two ~~.~~


in case the last published model’s score is higher than the currently training model’s score, the


pipeline will stop running right before uploading it to artifactory ~~.~~


in case this flag is enabled, the currently training model will be uploaded to artifactory


regardless to it’s auc score ~~.~~


when we’re calculating AUC to compare two models, we’re currently using only exe test sets,


and not dll ~~.~~


dev ~~-~~ disabled


a flag indicating that instead of uploading the artifacts to the ngav ~~-~~


pipeline/production/<un/managed> subfolder in artifactory, this model should be uploaded to


in case of developing, we’ll use the dev checkbox ~~.~~


in case of production, we’ll run with the feature flag off (default) ~~.~~


in case of a production model that didn’t turn out good enough, we’ll move it manually to the dev


subfolder in artifactory ~~.~~


the model ID’s (versions) are sequential and so there can’t be a situation where we have a


duplication in versions ~~.~~


